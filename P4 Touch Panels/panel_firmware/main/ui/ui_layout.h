#pragma once

/**
 * Shared UI layout helpers for any screen or component.
 * Include this header (or a specific `ui_layout_*.h`) from `main/ui/`.
 * Pulls in square-grid math (`ui_layout_grid`) and the full-screen template (`ui_screen_template`).
 */

#include "ui/ui_layout_grid.h"
#include "ui/ui_screen_template.h"
